services:
#  mongo:
#    image: mongo:8
#    restart: unless-stopped
#    volumes:
#      - mongo_data:/data/db
#    healthcheck:
#      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
#      interval: 5s
#      timeout: 5s
#      retries: 20

  shadow-signal:
    build: .
    restart: unless-stopped
    init: true
    env_file:
      - .env
#    depends_on:
#      mongo:
#        condition: service_healthy

#volumes:
#  mongo_data:
